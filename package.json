{
  "name": "docbot",
  "private": true,
  "workspaces": [
    "packages/backend",
    "packages/wordpress-plugin/docbot/admin"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=packages/backend",
    "build": "npm run build --workspace=packages/backend",
    "db:migrate": "npm run db:migrate --workspace=packages/backend",
    "db:generate": "npm run db:generate --workspace=packages/backend",
    "wp:build": "npm run build --workspace=packages/wordpress-plugin/docbot/admin"
  }
}
